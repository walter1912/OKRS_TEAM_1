import React from 'react';
import PropTypes from 'prop-types';

const HomePage = props => {
    return (
        <div>
            đây là trang home
        </div>
    );
};

HomePage.propTypes = {
    
};

export default HomePage;