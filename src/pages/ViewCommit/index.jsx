import React from 'react';
import PropTypes from 'prop-types';

const ViewCommit = props => {
    return (
        <div>
            
        </div>
    );
};

ViewCommit.propTypes = {
    
};

export default ViewCommit;