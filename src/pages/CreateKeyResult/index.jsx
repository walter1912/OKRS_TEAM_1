import React from 'react';
import PropTypes from 'prop-types';

const CreateKeyResult = props => {
    return (
        <div>
            
        </div>
    );
};

CreateKeyResult.propTypes = {
    
};

export default CreateKeyResult;