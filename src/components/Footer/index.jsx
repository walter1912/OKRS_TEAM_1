import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <div>
            đây là footer
        </div>
    );
};

Footer.propTypes = {
    
};

export default Footer;