import React from 'react';
import PropTypes from 'prop-types';

const Loading = props => {
    return (
        <div>
            loading.........
        </div>
    );
};

Loading.propTypes = {
    
};

export default Loading;